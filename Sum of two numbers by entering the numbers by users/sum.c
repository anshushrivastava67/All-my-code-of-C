#include <stdio.h>

int main() {
    int a,b,s;
    printf("enter the first number:");
    scanf("%d",&a);
    printf("enter the second number:");
    scanf("%d",&b);
    s=a+b;
    printf("sum of the number %d and %d is:%d",a,b,s);
    
    return 0;
}
